package Task2;

public class ClassForBoolean {
    static boolean insteadTrue(String number ){
        if(number.length()<7)
            return true;
        else
            return false;}

}
